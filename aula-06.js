let idade = 1;

// do while = ele passa primeiro a informação mesmo sendo falsa e depois ele verifica se deve passar de novo

do{
    console.log('Passou!', idade);
    idade = idade + 1;
}while (idade <= 18);